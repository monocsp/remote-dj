{
  "name": "remote-dj",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["packages/shared", "apps/server", "apps/web"],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev -w apps/server",
    "dev:web": "npm run dev -w apps/web",
    "dev:alt": "concurrently -n dev-server,dev-web -c magenta,cyan \"PORT=3101 REMOTE_DJ_ENV=dev REMOTE_DJ_DATA_FILE=$PWD/.data-dev/rooms.json npm run dev -w apps/server\" \"PORT=3100 npm run dev -w apps/web\"",
    "prd:build": "npm run build -w apps/web",
    "prd": "concurrently -n server,web -c blue,green \"PORT=3001 REMOTE_DJ_ENV=prd npm run start -w apps/server\" \"PORT=3000 npm run start -w apps/web\"",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "biome check . && npm run lint -w apps/web",
    "format": "biome check --write .",
    "test": "vitest run",
    "contract:export": "node scripts/export-contract.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "concurrently": "^9.1.0",
    "typescript": "^5.7.2",
    "vitest": "^3.2.0"
  }
}
