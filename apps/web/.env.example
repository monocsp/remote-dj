# NEXT_PUBLIC_SERVER_URL is OPTIONAL.
# By default the client derives the server URL at runtime from the page host
# (same hostname, port 3001) — no rebuild needed when you fork / change hosts.
#
# Set this only if your Socket.IO server lives somewhere else:
# NEXT_PUBLIC_SERVER_URL=https://my-remote-dj-server.example.com
