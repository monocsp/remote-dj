# Port the HTTP/Socket.IO server listens on
PORT=3001
# Network interface to bind (0.0.0.0 = all interfaces)
HOSTNAME=0.0.0.0
# Allowed CORS origin(s) for Socket.IO ('*' = allow any)
CORS_ORIGIN=*

# OPTIONAL: which environment this instance is (prd|dev). Tags diagnostic logs;
# defaults to dev. The prd/dev npm scripts set this for you.
REMOTE_DJ_ENV=

# OPTIONAL: room codes that must NEVER be auto-deleted by the empty-room sweep
# (comma-separated, case-insensitive). Use for a permanent "DJ home" room whose
# playlist must persist indefinitely even when empty over weekends/holidays.
# Example: PINNED_ROOMS=DOLOMO,LOBBY
PINNED_ROOMS=

# OPTIONAL: YouTube Data API v3 key. When set, the server checks
# `status.embeddable` at add time and BLOCKS embed-disabled videos on the FIRST
# add (the only reliable way). Without it, embed-disabled videos are caught at
# playback (the Player reports the error) and then blocked per-room thereafter.
# Get one: Google Cloud Console → enable "YouTube Data API v3" → Credentials →
# create API key (restrict it to YouTube Data API v3).
YOUTUBE_API_KEY=

# ── 한국 공휴일 자동 스킵 (schedule.skipHolidays) ──────────────────────────────
# 예약(스케줄)에서 "한국 공휴일에는 자동 재생 쉬기"를 켠 방은, 한국 공휴일에
# 자동 재생을 건너뛴다. 공휴일 데이터는 번들된 정적 목록(holidays.ts)을 기본으로
# 쓰고, 아래 값들로 보강한다.
#
# OPTIONAL: data.go.kr(공공데이터포털) "특일정보" 서비스 키. 설정하면 서버가
# 부팅 시 KASI getRestDeInfo 에서 올해+내년 공휴일을 받아 .data/holidays.json 에
# 저장하고, 정적 목록과 합쳐 사용한다(대체·임시공휴일까지 자동 포착). API 호출은
# 연 1회 수준으로 최소화된다(캐시가 해당 연도를 못 덮거나 ~300일 지나야 재요청).
# 키가 없거나 호출이 실패하면 번들 정적 목록만으로 정상 동작한다.
#   발급: data.go.kr → "특일정보" 검색 → 활용신청(자동승인) → "일반 인증키"
#   Encoding/Decoding 어느 형태든 그대로 넣으면 된다(서버가 자동 판별).
DATA_GO_KR_SERVICE_KEY=

# OPTIONAL: 정적 목록/ API 에 없는 날을 강제로 공휴일에 추가(쉼표구분, YYYY-MM-DD).
# 갑자기 지정된 임시공휴일(선거 임시공휴일 등)을 즉시 반영할 때. 서버 전역.
# 예: EXTRA_HOLIDAYS=2026-10-02,2027-03-02
EXTRA_HOLIDAYS=

# OPTIONAL: 공휴일에서 강제로 제외(그날은 평소처럼 재생). 쉼표구분 YYYY-MM-DD.
# 서버 전역. 예: HOLIDAY_OVERRIDES_OFF=2026-06-03
HOLIDAY_OVERRIDES_OFF=
